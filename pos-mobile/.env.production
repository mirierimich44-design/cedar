# Production build: React dist is served from the same domain as Laravel, so
# set to the Laravel app root. If React is served from a CDN/subdomain, set the full URL.
VITE_API_URL=https://serengeti.apexpos.co.ke
